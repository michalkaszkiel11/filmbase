class User {
    constructor(userId, userName, email, pass) {
        this.userId = userId;
        this.userName = userName;
        this.email = email;
        this.pass = pass;
    }
}

module.exports = User;
